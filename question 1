interface Sayable {
    String say(String mes);
}

public class string {
    public static void main(String[] args) {
        // Implementing Sayable interface using lambda expression
        Sayable sayable = (mes) -> "Good"+ mes;

        // Calling the say method
        String greeting = sayable.say("Morning");
        System.out.println(greeting);
    }
}
